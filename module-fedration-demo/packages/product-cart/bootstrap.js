import React from 'react';
import ReactDOM from 'react-dom/client';
import ProductCart from './src/ProductCart';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    
    <ProductCart />
  </React.StrictMode>
);

